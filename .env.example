# Local reference only. Do not put production secrets in this file.

# Required for bootstrapping a fresh production database.
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=replace-with-a-strong-password

# Logical bindings are managed by the runtime/hosting platform.
# DB
# CV_BUCKET
